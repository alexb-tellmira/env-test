on:
  workflow_dispatch:
jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    environment: prod
    secrets: inherit
    steps:
      - uses: ./.github/workflows/_deploy.yml
        with:
          environment: dev
          var: ${{ vars.VAR }}