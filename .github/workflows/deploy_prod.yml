on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: dev
    uses: ./.github/workflows/_deploy.yml
    secrets: inherit
    with:
      environment: prod
      var: ${{ vars.VAR }}