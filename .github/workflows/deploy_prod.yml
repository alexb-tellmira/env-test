on:
  workflow_dispatch:
jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: ./.github/workflows/_deploy.yml
        secrets: inherit
        with:
          environment: dev
          var: ${{ vars.VAR }}