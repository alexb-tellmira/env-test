on:
  workflow_dispatch:
jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    environment: prod
    uses: ./.github/workflows/_deploy.yml
    secrets: inherit
    with:
      environment: prod
      var: ${{ vars.VAR }}