on:
  push:
    branches:
      - '**'
jobs:
  deploy_dev:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: ./.github/workflows/_deploy.yml
        secrets: inherit
        with:
          environment: dev
          var: ${{ vars.VAR }}