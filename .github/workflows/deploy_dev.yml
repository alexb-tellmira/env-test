on:
  push:
    branches:
      - '**'
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: dev
    uses: ./.github/workflows/_deploy.yml
    secrets: inherit
    with:
      environment: dev
      var: ${{ vars.VAR }}